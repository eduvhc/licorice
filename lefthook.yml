# Binary resolution and file scrubbing happen inside a Node script so the same
# hook works on Windows (cmd) and Linux (sh) — the repo is authored on both.
pre-commit:
  commands:
    format-and-lint:
      run: node tools/hooks/pre-commit.mjs
